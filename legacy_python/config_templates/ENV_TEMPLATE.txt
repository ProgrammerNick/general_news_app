Copy to `.env` (project root) and fill in values. Do NOT commit your `.env`.

GEMINI_API_KEY=your_gemini_api_key_here
NEWSAPI_KEY=your_newsapi_key_here

# Optional: Neon Postgres (if empty, local JSON files are used)
DATABASE_URL=postgresql+psycopg://USER:PASSWORD@HOST/DB?sslmode=require

# TTS provider: gtts (default) or gcloud (Google Cloud Text-to-Speech)
TTS_PROVIDER=gtts
AUDIO_VOICE=en-US-Neural2-C
GOOGLE_APPLICATION_CREDENTIALS=
GOOGLE_TTS_SERVICE_ACCOUNT_JSON=

# App storage dirs
DATA_DIR=data
VECTORSTORE_DIR=data/vectorstore
SUMMARIES_DIR=data/summaries
TMP_DIR=data/tmp

# Generation settings
MODEL_NAME=gemini-1.5-flash
EMBEDDING_MODEL=text-embedding-004
MAX_ARTICLES=30
NEWS_LOOKBACK_HOURS=36
BRIEF_TARGET_WORDS=1200

